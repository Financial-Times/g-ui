/**
 * Footer component
 * ----------------
 * Gets styles from o-footer
 * Requires keeping markup up-to-date with whatever insanity they have in o-footer.
 */

import { h } from 'preact';

const gFooter = (props) => (
  <footer />
);

export default gFooter;
